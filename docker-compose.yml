services:
  claude:
    build:
      context: .
    image: claude-overnight:latest
    container_name: claude-overnight
    cap_add:
      - NET_ADMIN
      - NET_RAW
    volumes:
      - ~/.claude:/home/node/.claude
      - ~/.claude.json:/home/node/.claude.json
      - ${TASK_FILE}:/TASK.md:ro
      - ${REPO_PATH}:/target
    restart: "no"
